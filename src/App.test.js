import React from 'react';
import ReactDOM from 'react-dom';
import { shallow, mount } from 'enzyme';

describe('App', () => {
  it('should work as a fake test', () => {
    expect(true).toEqual(true);
  });
});
